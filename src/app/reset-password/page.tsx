import { Suspense } from 'react'
import { ResetPasswordForm } from '@/components/next/ResetPasswordForm'

export default function ResetPasswordPage() {
  return (
    <Suspense>
      <ResetPasswordForm />
    </Suspense>
  )
}
